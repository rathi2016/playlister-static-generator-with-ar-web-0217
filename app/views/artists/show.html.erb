<h2> <%= @artist.name %> </h2>
<h3>Genres-</h3>
<ul>
  <% @artist.genres.each do |genre| %>
  <li>
    <a href="genres/<%= genre.to_slug %>.html">
      <%= genre.name %>
    </a>
  </li>
  <% end %>
</ul>
